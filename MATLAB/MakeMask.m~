imSize = 140;

vecSize = [imSize,1];
p = 2;
pcgte = 0.5;
distType = 2;
radius = 0;
disp = 1;
[pdf,val] = genPDF(vecSize, p, pcgte,distType,radius,disp);

iter = 100;
tol=0.1;
[mask,stat,N] = samplingPattern(pdf,iter,tol);
fidure(1);
Mask = co
imshow(